.mdc-layout-grid
  .mdc-layout-grid__inner
    .mdc-layout-grid__cell--span-12
      %h1= @text.title
      %ul
        - @books.each do |book|
          %li= link_to book.book_number, "/authors/#{@author.short_name.downcase}/#{@text.title.downcase.gsub(' ', '-')}/#{book.book_number}"
